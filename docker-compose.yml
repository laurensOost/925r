version: '3'

services:
  ldap:
    image: osixia/openldap:1.1.7
  web:
    build: .
    command: bash -c "python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/code
    ports:
      - "8088:8000"
    depends_on:
      - ldap
